using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Covid : MonoBehaviour
{
    
   

    //private int d = 3;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //for (int i = 0; i < DrawLine.gameObjects.Count; i++)
        //{
        //    if (DrawLine.gameObjects[i] != this.gameObject)
        //    {
        //        if (Vector2.Distance(DrawLine.gameObjects[i].transform.position, this.transform.position) <= d)
        //        {
        //            if (DrawLine.gameObjects[i].GetComponent<Covid>() == null)
        //            {
        //                DrawLine.gameObjects[i].GetComponent<Move>().IsStart = true;


        //                //Debug.Log("isstart");
        //            }
                    


        //        }
        //        else
        //        {
        //            DrawLine.gameObjects[i].GetComponent<Move>().IsEnd = true;
        //            //Debug.Log("isend");
        //        }
               
        //    }


        //}
    }
}
